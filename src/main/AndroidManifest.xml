<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.honorsmobileapps.matthewkelleher.kahoot21">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LeaderboardScreen"></activity>
        <activity android:name=".PlayingQuizScreen" />
        <activity android:name=".SignInScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainScreen" />
        <activity
            android:name=".MyQuizzesScreen"
            android:label="@string/title_activity_my_quizzes" />
        <activity
            android:name=".CreateScreen"
            android:label="@string/title_activity_create_screen"
            android:theme="@style/AppTheme" />
    </application>

</manifest>